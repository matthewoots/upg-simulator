//Do not edit! This file was generated by Unity-ROS MessageGeneration.
using System;
using System.Linq;
using System.Collections.Generic;
using System.Text;
using Unity.Robotics.ROSTCPConnector.MessageGeneration;

namespace RosMessageTypes.Mavros
{
    public class MESCStatus : Message
    {
        public const string RosMessageName = "mavros_msgs/ESCStatus";

        //  ESCStatus.msg
        // 
        // 
        //  See mavlink message documentation here:
        //  https://mavlink.io/en/messages/common.html#ESC_STATUS
        public Std.MHeader header;
        public MESCStatusItem[] esc_status;

        public MESCStatus()
        {
            this.header = new Std.MHeader();
            this.esc_status = new MESCStatusItem[0];
        }

        public MESCStatus(Std.MHeader header, MESCStatusItem[] esc_status)
        {
            this.header = header;
            this.esc_status = esc_status;
        }
        public override List<byte[]> SerializationStatements()
        {
            var listOfSerializations = new List<byte[]>();
            listOfSerializations.AddRange(header.SerializationStatements());
            
            listOfSerializations.Add(BitConverter.GetBytes(esc_status.Length));
            foreach(var entry in esc_status)
                listOfSerializations.Add(entry.Serialize());

            return listOfSerializations;
        }

        public override int Deserialize(byte[] data, int offset)
        {
            offset = this.header.Deserialize(data, offset);
            
            var esc_statusArrayLength = DeserializeLength(data, offset);
            offset += 4;
            this.esc_status= new MESCStatusItem[esc_statusArrayLength];
            for(var i = 0; i < esc_statusArrayLength; i++)
            {
                this.esc_status[i] = new MESCStatusItem();
                offset = this.esc_status[i].Deserialize(data, offset);
            }

            return offset;
        }

        public override string ToString()
        {
            return "MESCStatus: " +
            "\nheader: " + header.ToString() +
            "\nesc_status: " + System.String.Join(", ", esc_status.ToList());
        }
    }
}
