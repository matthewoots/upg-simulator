//Do not edit! This file was generated by Unity-ROS MessageGeneration.
using System;
using System.Linq;
using System.Collections.Generic;
using System.Text;
using Unity.Robotics.ROSTCPConnector.MessageGeneration;

namespace RosMessageTypes.Mavros
{
    public class MWaypointList : Message
    {
        public const string RosMessageName = "mavros_msgs/WaypointList";

        //  WaypointList.msg
        // 
        //   :current_seq:   seq nr of currently active waypoint
        //                   waypoints[current_seq].is_current == True
        // 
        //   :waypoints:     list of waypoints
        public ushort current_seq;
        public MWaypoint[] waypoints;

        public MWaypointList()
        {
            this.current_seq = 0;
            this.waypoints = new MWaypoint[0];
        }

        public MWaypointList(ushort current_seq, MWaypoint[] waypoints)
        {
            this.current_seq = current_seq;
            this.waypoints = waypoints;
        }
        public override List<byte[]> SerializationStatements()
        {
            var listOfSerializations = new List<byte[]>();
            listOfSerializations.Add(BitConverter.GetBytes(this.current_seq));
            
            listOfSerializations.Add(BitConverter.GetBytes(waypoints.Length));
            foreach(var entry in waypoints)
                listOfSerializations.Add(entry.Serialize());

            return listOfSerializations;
        }

        public override int Deserialize(byte[] data, int offset)
        {
            this.current_seq = BitConverter.ToUInt16(data, offset);
            offset += 2;
            
            var waypointsArrayLength = DeserializeLength(data, offset);
            offset += 4;
            this.waypoints= new MWaypoint[waypointsArrayLength];
            for(var i = 0; i < waypointsArrayLength; i++)
            {
                this.waypoints[i] = new MWaypoint();
                offset = this.waypoints[i].Deserialize(data, offset);
            }

            return offset;
        }

        public override string ToString()
        {
            return "MWaypointList: " +
            "\ncurrent_seq: " + current_seq.ToString() +
            "\nwaypoints: " + System.String.Join(", ", waypoints.ToList());
        }
    }
}
